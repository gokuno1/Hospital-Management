spring:
  datasource:
    url: jdbc:mysql://localhost:3306/userservice
    username: root
    password: root  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true  
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        
  security:
    user:
      name: user
      password: user