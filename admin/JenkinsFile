pipeline
{
	agent any
	
	stages
	{
		stage('Build')
		{
			steps
			{
				bat 'mvn -f admin/pom.xml clean package'
			}
		}
		stage('Build Docker Image')
		{
			steps
			{
				bat 'docker build -t myFirstProject .' 
			}
		}
		stage('Run Image')
		{
			steps
			{
				echo 'Running Docker Image created above'
			}
		}
	}
}
